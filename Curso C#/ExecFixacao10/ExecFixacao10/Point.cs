namespace Course
{
    struct Point
    {
        public double dX;
        public double dY;

        public override string ToString()
        {
            return "(" + dX + ", " + dY + ")";
        }
    }
}